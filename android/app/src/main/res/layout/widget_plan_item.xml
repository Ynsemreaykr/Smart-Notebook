<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/plan_item_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="8dp"
    android:background="@drawable/widget_item_bg">

    <!-- Indent Spacers -->
    <FrameLayout
        android:id="@+id/widget_indent_1"
        android:layout_width="16dp"
        android:layout_height="1dp"
        android:visibility="gone" />
    <FrameLayout
        android:id="@+id/widget_indent_2"
        android:layout_width="32dp"
        android:layout_height="1dp"
        android:visibility="gone" />
    <FrameLayout
        android:id="@+id/widget_indent_3"
        android:layout_width="48dp"
        android:layout_height="1dp"
        android:visibility="gone" />



    <!-- ok button ↓/↑ -->
    <TextView
        android:id="@+id/plan_item_ok_btn"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="6dp"
        android:background="#1AFFFFFF"
        android:text="↓"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:textStyle="bold"
        android:gravity="center"
        android:visibility="gone" />

    <!-- Accent accent bar -->
    <FrameLayout
        android:id="@+id/plan_item_accent"
        android:layout_width="4dp"
        android:layout_height="24dp"
        android:background="#7C83E8" />

    <!-- Title / Content -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="8dp">

        <TextView
            android:id="@+id/plan_item_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>

    <!-- Percent badge -->
    <TextView
        android:id="@+id/plan_item_percent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:background="@drawable/widget_percent_bg"
        android:paddingHorizontal="6dp"
        android:paddingVertical="2dp"
        android:textColor="#2DD4BF"
        android:textSize="11sp"
        android:textStyle="bold"
        android:visibility="gone" />

</LinearLayout>
