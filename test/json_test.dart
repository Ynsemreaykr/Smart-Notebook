import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test json', () {
    // var line = SmoothLine();
    // line.paint = Paint()..color = Colors.black;
    // line.pushPoint(Offset(10, 10));
    // line.pushPoint(Offset(20, 20));
    // print('Line: ${jsonEncode(line.toJson())}');
  });
}
